package com.codered.repository;

import com.codered.model.DonorOutreach;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DonorOutreachRepository extends JpaRepository<DonorOutreach, Long> {
}
spring.application.name=codered-backend
server.port=8080

# PostgreSQL (Supabase)
spring.datasource.url=jdbc:postgresql://aws-1-ap-northeast-2.pooler.supabase.com:6543/postgres?sslmode=require&prepareThreshold=0
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=postgres.btkcemaclntezjrkdgqc
spring.datasource.password=9sMtricq6CwErBSh

# JPA — 'create' drops and recreates tables on each startup (change to 'update' once data should persist)
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# JWT
jwt.secret=codeREDSecretKey2025codeREDSecretKey2025codeREDSecretKey2025!
jwt.expiration=86400000
jwt.refresh-expiration=604800000

# CORS
cors.allowed-origins=http://localhost:5173

# Gemini AI Configuration
gemini.api.key=gemini key
gemini.api.url=https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash-lite:generateContent